name = "blvm-lightning"
version = "0.1.3"
description = "Lightning Network payment processor module for blvm-node"
author = "Bitcoin Commons Team"
entry_point = "blvm-lightning"

capabilities = [
    "read_blockchain",
    "subscribe_events",
]

# CI fills url/sha256 on release — do not treat empty entries as real installs.
